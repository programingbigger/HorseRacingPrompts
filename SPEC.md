# HorseRacingPrompts — プロジェクト仕様書

**最終更新：** 2026年05月30日  
**目的：** 単勝馬券の回収率をプラスにする自己改善型AIシステムの構築

---

## 1. プロジェクト概要

生成AI（Claude）を使って競馬の単勝馬券予想を行い、**予想 → 結果検証 → 自己フィードバック → 判断軸の改善** というサイクルを回すことで、長期的に回収率をプラスにすることを目指すリポジトリ。

### 基本方針

```
回収率プラス = 的中率 × オッズ > 1.0
```

単純に当てにいくのではなく、**「過小評価されている馬（オッズ妙味がある馬）」を見つける** ことを最優先とする。

---

## 2. ファイル構成（現状 v1）

```
HorseRacingPrompts/
│
├── .claude/
│   ├── agents/
│   │   └── keiba-tansho-picker.md      # Claude Agentの定義
│   ├── skills/
│   │   └── keiba-joho-shushu/
│   │       └── SKILL.md                # 情報収集スキルの定義
│   └── agent-memory/
│       └── keiba-tansho-picker/        # エージェントの記憶（蓄積型）
│
├── prompts/
│   └── race_analysis_v1.md             # 手動コピペ用のプロンプトテンプレート
│
├── {競馬場名}/                          # 情報収集の出力先（例: 東京競馬場/）
│   └── {YYYY年MM月}/
│       └── {コース}_{距離}m_{レース名}.md
│
├── eval_review.html                    # eval結果を人間がレビューするためのUI
├── keiba-joho-shushu.skill             # スキルのバイナリ
├── README.md
├── SPEC.md                             # 本ファイル
└── LICENSE
```

---

## 3. 各コンポーネントの役割

### 3-1. `keiba-tansho-picker` エージェント（`.claude/agents/`）

| 項目 | 内容 |
|------|------|
| **役割** | ユーザーがnetkeibaのURLを渡すと、自動で情報収集〜予想〜推奨を行うエージェント |
| **モデル** | Claude Opus（高精度分析のため） |
| **使い方** | Claude Codeにnetkeibaの出走表URLを貼るだけで起動 |
| **出力** | 推定的中率・期待値・推奨単勝馬をユーザーにチャットで提示 |
| **記憶** | `.claude/agent-memory/keiba-tansho-picker/` に経験を蓄積 |

### 3-2. `keiba-joho-shushu` スキル（`.claude/skills/`）

| 項目 | 内容 |
|------|------|
| **役割** | netkeibaからブラウザ経由で情報を収集し、マークダウンファイルとして保存するスキル |
| **収集データ** | ①過去レース傾向（data_top / past10）②血統・コース適性（db.netkeiba）③リアルタイムオッズ・馬体重 |
| **保存先** | `{競馬場名}/{YYYY年MM月}/{コース}_{距離}m_{レース名}.md` |
| **使い方** | `/keiba-joho-shushu {URL}` で手動起動、またはエージェントから自動起動 |

### 3-3. 情報収集フォルダ（`東京競馬場/` 等）

| 項目 | 内容 |
|------|------|
| **役割** | レースごとの生データ・傾向データを保存する |
| **内容** | 出走馬一覧・オッズ・過去傾向・血統適性（純粋な情報素材） |
| **注意** | AIの予想・推奨文は**ここに入れない**（後述 v2 で分離） |

### 3-4. `prompts/` ディレクトリ

| 項目 | 内容 |
|------|------|
| **役割** | ChatGPT・Gemini等への手動コピペ用プロンプトテンプレート集 |
| **現状** | `race_analysis_v1.md` のみ存在 |

### 3-5. `eval_review.html`

| 項目 | 内容 |
|------|------|
| **役割** | AIの予想結果を人間がレビューしてGood/Badを評価するためのブラウザUI |
| **仕組み** | xlsxファイルを読み込んで入力→出力のペアを1件ずつ表示、人間がフィードバックを記録 |
| **用途** | 「このスキルは正しく動いているか？」の定性評価に使う |

---

## 4. 現状のデータフロー

```
ユーザー
  │ netkeibaのURL を貼る
  ▼
keiba-tansho-picker エージェント
  │ keiba-joho-shushu スキルを呼び出す
  ▼
情報収集（Claude in Chrome でnetkeibaを巡回）
  │ 出走表 / data_top / past10 / db.netkeiba
  ▼
{競馬場名}/{年月}/芝_2500m_目黒記念.md に保存
  │
  ▼（同ファイル内に予想も含まれている ← 問題点）
ユーザーへの推奨チャット出力
```

**現状の課題：** 情報（素材）と予想（分析結果）が同一ファイルに混在しており、フィードバックループが回せない。

---

## 5. 次回改修内容（TODO）

### 🔵 優先度 HIGH

---

#### TODO-01：フォルダ構造の分離（情報収集 vs AI予想）

**背景：** 現在の情報収集ファイルにAIの推奨文が混在している。情報の純度を保つため、フォルダを分離する。

**改修後のフォルダ構造：**

```
HorseRacingPrompts/
│
├── data/                               # ← 純粋な情報・素材（AIの判断なし）
│   └── {競馬場名}/
│       └── {YYYY年MM月}/
│           └── {コース}_{距離}m_{レース名}.md
│
├── predictions/                        # ← AIの予想・推奨（⭐️ 新設）
│   └── {競馬場名}/
│       └── {YYYY年MM月}/
│           └── {コース}_{距離}m_{レース名}_prediction.md
│
└── feedback/                           # ← 自己フィードバック（⭐️ 新設）
    └── {競馬場名}/
        └── {YYYY年MM月}/
            └── {コース}_{距離}m_{レース名}_feedback.md
```

**`data/` に入れるもの（純粋な素材）：**
- 出走馬一覧（馬番・馬名・騎手・斤量・オッズ）
- 馬体重・増減
- 過去レース傾向（枠順・脚質・人気別成績）
- 各馬の血統・コース成績
- 過去10年の結果

**`predictions/` に入れるもの（AIの判断）：**
- 期待値スコア・推定的中率
- 推奨馬の選定理由
- 買い方アドバイス
- レース後に結果を追記できるフィールドを設ける

---

#### TODO-02：自己反省ループの実装（predictions → feedback）

**背景：** レースが終わったら「予想は正しかったか？」を自動で振り返り、次回の判断軸の改善につなげたい。

**フィードバックの観点（整理）：**

| # | 観点 | 具体的な問い |
|---|------|-------------|
| 1 | **的中精度** | 推奨した馬は何着だったか？期待値は実際に1.0を超えていたか？ |
| 2 | **見逃し分析** | 実際の1着馬を予想に入れていたか？入れていなかった場合、なぜ見落としたか？ |
| 3 | **判断根拠の検証** | 推奨理由として挙げたデータポイント（枠順・血統・騎手等）は実際に有効だったか？ |
| 4 | **オッズ妙味の検証** | 低オッズ馬（3倍以下）を本命にした場合、長期回収率は悪化していないか？ |
| 5 | **見逃しパターン** | 荒れるレースで固い予想をしていないか？人気馬に引きずられていないか？ |

**フィードバックファイルの書式案：**

```markdown
# {レース名} 自己フィードバック

**レースID：** {RID}
**予想ファイル：** [link to prediction file]
**実際の結果：** {記入}

## 結果照合
| 推奨馬 | 推定的中率 | オッズ | 着順 | 期待値実績 |
|---|---|---|---|---|
| ハーツコンチェルト | 15% | 8.8倍 | {結果} | {実績} |

## 当たった判断軸 ✅
- （例）武井亮厩舎×東京芝2500mの相性データ → 実際に上位入線

## 外れた判断軸 ❌
- （例）レーン騎手の目黒記念成績 → 今回は機能せず

## 次回への改善アクション
- ...
```

---

#### TODO-03：判断軸ドキュメントの自動生成・更新

**背景：** フィードバックを積み重ねることで「これなら当たる」という判断軸を自動でアップデートしたい。

**仕組み：**

1. `feedback/` フォルダに蓄積されたファイルを読み込む
2. 「有効だった判断軸」「無効だった判断軸」を集計
3. `judgment_criteria.md`（判断軸マスタードキュメント）を自動更新

**`judgment_criteria.md` の構造案：**

```markdown
# 単勝予想 判断軸マスタードキュメント
（自動更新: keiba-tansho-picker エージェントが feedback/ を分析して更新）

## A. 信頼度の高い判断軸（実績あり）
| 判断軸 | 適用コース | サンプル数 | 的中率 | 備考 |
|---|---|---|---|---|
| 武井亮厩舎 × 東京芝2500m | 東京芝2500 | 3 | 67% | ... |

## B. 要検証の判断軸（サンプル少）
...

## C. 無効と判明した判断軸
...

## D. コース別・条件別の重み付けテーブル
...
```

---

### 🟡 優先度 MEDIUM

---

#### TODO-04：`keiba-joho-shushu` スキルの改修

**変更点：** 情報収集と予想の分離に対応してスキルを改修する。

- **保存先を `data/` に変更**（現在はルート直下の `{競馬場名}/` に保存）
- **AIの推奨文（「推奨馬まとめ」セクション）を削除**し、情報収集に徹する
- 収集する情報はあくまで「素材」のみ：
  - 出走表データ
  - 過去傾向データ
  - 血統・コース成績
  - オッズ・馬体重
- 分析・推奨は `keiba-tansho-picker` エージェントの責務に移譲

---

#### TODO-05：`predictions/` 出力のスキル化

**新規スキルまたはエージェントの動作として追加。**

- `data/` ファイルを読み込んで期待値計算・推奨馬選定を行う
- 結果を `predictions/` に保存する
- レース後に結果を記入できる空フィールドを含む

---

#### TODO-06：eval_review.html との連携強化

**現状：** eval_review.htmlは手動でxlsxを読み込む形式。

**改修案：** `predictions/` と `feedback/` の内容を自動的にレビューUIに流し込む仕組みを作る。

---

## 6. 将来のシステム全体像（v2 ターゲット）

```
ユーザー
  │ netkeibaのURL を貼る
  ▼
keiba-tansho-picker エージェント
  │
  ├─ [Step 1] keiba-joho-shushu スキル
  │    └─ data/{競馬場}/{年月}/{レース名}.md に情報保存
  │
  ├─ [Step 2] judgment_criteria.md を読み込む
  │    └─ 蓄積された判断軸でスコアリング
  │
  ├─ [Step 3] 期待値計算・推奨選定
  │    └─ predictions/{競馬場}/{年月}/{レース名}_prediction.md に保存
  │
  └─ [Step 4] ユーザーへ推奨をチャット出力

（レース後）
  │ ユーザーが結果を報告 or 自動取得
  ▼
keiba-tansho-picker エージェント
  ├─ prediction ファイルと結果を照合
  ├─ feedback/{競馬場}/{年月}/{レース名}_feedback.md を生成
  └─ judgment_criteria.md を自動更新
```

---

## 7. 免責事項

- 本リポジトリで提供するプロンプト・スキル・エージェントは、回収率向上を目的としていますが、的中を保証するものではありません。
- 競馬はギャンブルです。余裕資金の範囲内でお楽しみください。
